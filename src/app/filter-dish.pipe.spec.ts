import { FilterDishPipe } from './filter-dish.pipe';

describe('FilterDishPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDishPipe();
    expect(pipe).toBeTruthy();
  });
});
