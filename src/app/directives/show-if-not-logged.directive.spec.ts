import { ShowIfNotLoggedDirective } from './show-if-not-logged.directive';

describe('ShowIfNotLoggedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfNotLoggedDirective();
    expect(directive).toBeTruthy();
  });
});
